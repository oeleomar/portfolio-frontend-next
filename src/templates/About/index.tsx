import { AboutProps } from "../../../types/portfolio";
import * as Styled from "./styles";

export const About = ({ ...data }: AboutProps) => {
  return <Styled.Wrapper>About</Styled.Wrapper>;
};
