import { ExperiencesProps } from "../../../types/portfolio";
import * as Styled from "./styles";

export const Experiences = ({ ...data }: ExperiencesProps[]) => {
  return <Styled.Wrapper>Experiences</Styled.Wrapper>;
};
